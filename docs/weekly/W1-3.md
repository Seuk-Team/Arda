# W1-3 — 실측 결과 반영: 목업은 Day 1에 끝난다, 전원 백엔드로

> **발생** 08/24 · **요청** 팀장 · **담당** 팀원 4 전원 + 팀장 · **분량** ±0점 (교체)
> **본 계획서**: [W1.md](W1.md) · **선행**: [W1-2](W1-2.md)
> **이 문서는 W1을 대체하지 않는다.** 아래 표에 적힌 것만 바뀐다.

## 왜 생겼나 — 계획이 실제보다 3배 무겁게 잡혀 있었다

[W1.md](W1.md)를 배포한 지 **45분 만에** 이렇게 됐다.

| PR | 담당 | 산출 | 생성 시각 |
|---|---|---|---|
| [#3](https://github.com/Team-Seuk/Arda/pull/3) | woojeongalex | A1 로그인 (107줄) | 01:56 |
| [#4](https://github.com/Team-Seuk/Arda/pull/4) | cloverky | H1 통합검색 (337줄) | 02:00 |
| [#5](https://github.com/Team-Seuk/Arda/pull/5) | cloverky | 모바일 리스트 (267줄) | 02:01 |

**cloverky는 1주차 배정분 5점 전체를 한 시간 안에 끝냈다.** W1은 그것을 Day 1~4에 걸쳐 하도록 잡아뒀다.

품질도 문제없다 — 4개 파일 전부 `:root` 토큰 블록이 `mockup.html`과 **바이트 단위로 동일**(`f3f1a97f` / 27개)하고, **새로 만든 색이 하나도 없다.** H1은 지시서가 금지한 퍼널 레일·상세 패널을 정확히 넣지 않았고, 모바일은 44px 터치 타깃·`overflow-x`·`ellipsis`를 다 갖췄다.

**결론: "목업 8장에 한 주"라는 전제가 틀렸다. 하루면 끝난다.**

## 할 일

### 1. 남은 목업 5장을 Day 1~2에 끝낸다

| 화면 | 담당 | 상태 |
|---|---|---|
| A1 로그인 · H1 통합검색 · 모바일 | 팀원1 · 팀원2 | **완료** |
| **C1 지원폼** | 팀원1 | **최우선** — ERD 확정에 필요 |
| **E1 평가현황** | 팀원4 | **최우선** — ERD 확정에 필요 |
| B1 공고목록 | 팀원3 | Day 1~2 |
| A2 설정 | 팀원4 | E1 다음 |
| 대시보드 | — | **보류** (범위 밖 — [W1-2](W1-2.md) 참조) |

### 2. ERD를 C1 · E1 머지 즉시 확정한다 — 팀장

[W1-2](W1-2.md)에서 "목표 Day 3"으로 잡았지만, 이 속도면 **오늘 안에 가능하다.**

- [ ] C1 · E1 머지 → 그 화면의 실제 필드로 [01-erd.md](../01-erd.md) 검증 → 상태 줄을 `확정 vX.X · 날짜`로 교체

### 3. 목업이 끝난 사람은 즉시 다음으로 — 전원

| 팀원 | 목업 끝나면 바로 |
|---|---|
| 팀원1 | C1 완료 후 → **F1 S3 presigned 업로드** (AWS 계정 나오면) / 그 전엔 **G1 메일 문구** |
| 팀원2 | **이미 끝남** → 지금 **J7 파트A 재료 사전**(선행 없음) → ERD 확정 후 **J7 파트B 생성 스크립트** |
| 팀원3 | B1 완료 후 → **B2 공고 CRUD API** (ERD 확정 후) |
| 팀원4 | E1 · A2 완료 후 → **J1 Docker compose** (ERD 무관, 앱 뼈대만 필요) |

**대기 시간이 생기면 [J7 파트A](../tasks/J7-더미데이터-생성기.md)로 간다.** 선행조건이 없고 배치로 쪼개져 있어 몇 명이 동시에 해도 충돌하지 않는다.

## 이제 병목은 팀장 한 명이다

팀원이 45분에 한 주치를 끝내는데 **다음에 할 일의 지시서가 없으면 전원이 논다.**
이 프로젝트의 속도는 이제 코딩 속도가 아니라 **지시서 생산 속도**가 결정한다.

- [ ] **오늘 안에** `docs/tasks/B2-공고-CRUD-API.md` · `docs/tasks/J1-docker-compose.md`
- [ ] **ERD 확정 직후** 2주차 지시서 일괄 — F1/F2 S3 · G2/G3 메일 큐 · H1/H2 검색 API · D1/D4 지원자 API · J8 테스트
- [ ] 지시서는 **초안을 Claude로 뽑고 검수만 한다.** [_template.md](../tasks/_template.md)와 기존 지시서 하나를 예시로 주면 형식이 맞는 초안이 나온다. 손으로 쓰면 개당 한 시간, 검수만 하면 10분이다
- [ ] **목업 PR 리뷰는 5분 컷** — ① 목업과 나란히 비교 ② `:root` 토큰 해시가 `f3f1a97f`(27개)인가 ③ `git status`에 지시서 밖 파일이 없는가. 코드를 줄 단위로 읽지 않는다

```bash
cd frontend/mockups && for f in mockup*.html; do echo "$(sed -n '/^:root{/,/^}/p' "$f" | md5sum | cut -c1-8)  $f"; done
```

## 누구 몫을 줄였나

**아무도 줄지 않았다. 오히려 늘었다.** 목업이 예상보다 빨리 끝나 생긴 시간을 백엔드 착수로 채운다.
[W1.md](W1.md)의 4주차 이월분(QA 시나리오 · 발표 뼈대)은 [W1-2](W1-2.md)의 결정을 그대로 유지한다.

## 충돌 확인

- [x] W1 5장 표의 다른 사람 파일과 겹치지 않는다

| 팀원 | 이번 주 파일 |
|---|---|
| 팀원1 | `mockup-login.html`(완료) · `mockup-apply.html` · `docs/email-templates.md` |
| 팀원2 | `mockup-applicants.html`(완료) · `mockup-mobile.html`(완료) · `backend/scripts/seed/` |
| 팀원3 | `mockup-postings.html` · `backend/app/` 공고 API |
| 팀원4 | `mockup-evaluations.html` · `mockup-settings.html` · `docker-compose.yml` · `Dockerfile` |

`backend/app/`은 팀원3만, `backend/scripts/`는 팀원2만, 컨테이너 파일은 팀원4만 건드린다. **여전히 겹치는 파일이 없다.**

## 별건 — `mockup.html`의 색 리터럴 (금요일 처리)

토큰 검사 중 발견했다. **베낄 원본이 "색을 직접 쓰지 않는다" 규칙을 스스로 어기고 있다.**

| 종류 | 개수 | 예 |
|---|---|---|
| 토큰이 있는데 리터럴로 씀 | **6개 · 31회** | `#DCE3D4`(17회) → `var(--sunken-hover)` · `#EBF0E5`(9회) → `var(--bg-sunken)` · `#2C5218` → `var(--leaf-strong)` |
| 토큰 자체가 없음 | **7개 · 11회** | `#8CC63F` · `#C7D0BC` · `#EFF7E2` · `#F6F9F2` · `#8A9284` · `#AEB6A8` · `#C9CFC3` |

전원이 이걸 복사하므로 모든 목업에 번진다. **지금 고치지 않는다** — 화면 결과는 동일하고, 지금 `mockup.html`을 건드리면 작업 중인 C1·E1·B1·A2에 사본 불일치가 생긴다. 기계적 치환이라 나중에 해도 비용이 같다.

- [ ] **금요일에 일괄 처리** — 토큰 없는 7개는 이름을 정해야 하므로 팀장 결정이 필요하다. React 전환(2~3주차) 전에는 반드시 끝내야 한다
