# [발표] 최종 발표 뼈대 · 시연 시나리오 초안

> 담당: (이름) · 브랜치: `docs/presentation-outline`

## 배경

최종 발표는 전원이 한 파트씩 말한다. 목차와 시연 순서를 미리 잡아두면 마지막 주에 자료 만들 때 헤매지 않고, 각자 "면접에서 말할 것"이 발표 파트와 일치하게 된다.

## 완료 조건

- [ ] `docs/presentation-outline.md`에:
  - 발표 목차(10~15분 기준, 섹션별 예상 시간)
  - 파트 배분표(역할 A~E ↔ 발표 섹션 매핑, 이름은 빈칸)
  - 시연 시나리오: 어떤 순서로 무엇을 보여줄지 (외부 지원 → 칸반/목록 → 단계 이동 → 메일 수신 → 검색 성능)
  - 시연 사고 대비: 데모 실패 시 대체(스크린샷/영상) 계획 한 줄
- [ ] 기술 어필 포인트가 각 역할의 "면접에서 말할 것"([docs/04-team.md](../04-team.md))과 1:1로 연결

## 참고 문서

- [docs/04-team.md](../04-team.md) — 역할·면접 멘트
- [docs/adr/](../adr/) — "왜 안 썼는가" 스토리 (발표 차별화 포인트)

## Claude에게 시키기

```
docs/tasks/발표-뼈대.md를 읽고 완료 조건대로 docs/presentation-outline.md를 작성해줘.
docs/04-team.md의 "면접에서 말할 것"과 docs/adr/의 결정들이 발표 안에 자연스럽게
배치되도록. 심사위원이 개발자라고 가정. 바로 커밋하지 마.
```

## 검증 방법

목차만 보고 팀장이 "이 순서로 15분에 되겠는지" 확인. 시연 시나리오는 화면 지도(05-design 0.5)와 어긋나는 단계가 없는지 대조.

## 다음 할 일 — PR을 올린 뒤

작업이 끝나면 **아래를 Claude에게 그대로 붙여넣는다.** 다음 할 일을 스스로 정하지 않는다 — 이미 승인된 목록에서만 꺼낸다.

```
방금 이 작업의 PR을 올렸다.

1. docs/weekly/ 의 이번 주 문서를 전부 읽어라.
   W1.md 가 본 계획이고 W1-2 · W1-3 … 번호가 큰 것이 나중에 정해진 것이다. 뒤엣것이 우선한다.
2. 그 문서들에서 내 다음 작업을 찾아 알려줘라.
3. 내 다음 작업이 표에 없으면 docs/tasks/J7-더미데이터-생성기.md 의 파트 A 를 읽고,
   backend/scripts/seed/ 에 아직 만들어지지 않은 배치가 무엇인지 알려줘라.

규칙:
- 목록에 없는 일을 스스로 만들어내지 마라.
- 이 단계에서는 파일을 하나도 고치지 마라. 찾아서 알려주기만 해라.
- 다음 작업의 지시서가 docs/tasks/ 에 없으면 "지시서 없음"이라고 말해라.
```

**지시서가 없는 작업은 시작하지 않는다.** 팀 채널에 요청해서 받는다.

## PR 체크리스트

- [ ] 파트 배분표에 다섯 역할 전부 매핑됨
- [ ] 코드·스키마 변경 없음 (문서만)
