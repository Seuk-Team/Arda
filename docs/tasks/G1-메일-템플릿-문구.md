# [G1] 단계별 메일 템플릿 문구 4종

> 담당: (이름) · 브랜치: `feat/g1-email-templates`

## 배경

지원자가 단계를 이동하면 자동으로 메일이 발송된다(G1). 발송 시스템은 다른 사람이 만들지만, **무엇을 보낼지(문구)**가 먼저 있어야 한다. 회사가 실제로 보내도 어색하지 않은 수준의 한국어 문구가 목표다.

## 완료 조건

- [ ] `docs/email-templates.md`에 템플릿 4종: ① 서류 합격(면접 안내 포함) ② 면접 결과 합격 ③ 최종 합격 ④ 불합격
- [ ] 각 템플릿에 제목 + 본문, 변수는 `{{지원자명}}` `{{공고명}}` `{{회사명}}` `{{면접일시}}` 형식으로 표기
- [ ] 불합격 문구는 정중하고 담백하게 — 과한 위로·사족 금지
- [ ] 전 템플릿 존댓말·톤 통일, 이모지·느낌표 남발 금지

## 참고 문서

- [docs/00-overview.md](../00-overview.md) — 서비스 맥락
- 단계 이름: 지원 접수 / 서류 검토 / 면접 / 최종 합격 / 불합격 ([docs/01-erd.md](../01-erd.md))

## Claude에게 시키기

repo 루트에서 Claude Code를 열고 아래를 붙여넣는다.

```
docs/tasks/G1-메일-템플릿-문구.md를 읽고, 완료 조건대로 docs/email-templates.md를 작성해줘.
채용 지원자에게 보내는 공식 메일이니 회사가 실제 발송해도 어색하지 않은 한국어로.
초안을 보여주면 내가 검토 후 수정 지시할게. 바로 커밋하지 마.
```

```
마지막 단계 — 여기서 멈추지 마라.

위 작업을 끝내고 PR 을 올렸으면, 이어서 다음 할 일을 스스로 찾아 그대로 수행해라.

1. docs/weekly/ 의 이번 주 문서를 전부 읽어라.
   W1.md 가 본 계획이고 W1-2 · W1-3 … 번호가 큰 것이 나중에 정해진 것이다. 뒤엣것이 우선한다.
2. 거기서 내 다음 작업을 찾아라. 없으면 docs/tasks/J7-더미데이터-생성기.md 의 파트 A 에서
   backend/scripts/seed/ 에 아직 없는 배치를 가져가라.
3. 찾은 작업의 지시서를 docs/tasks/ 에서 열고, 선행 조건을 먼저 확인해라.
   선행 조건이 안 채워졌으면 거기서 멈추고 무엇이 막혔는지 알려줘라.
4. 선행 조건이 채워졌으면 새 브랜치를 만들고 그 지시서의 "Claude에게 시키기" 를
   0단계부터 순서대로 수행해라. 지시서 하나 = PR 하나다.
5. 그 작업도 끝나면 1번으로 돌아가 반복해라.

지키는 것:
- 지시서가 있는 작업만 한다. docs/tasks/ 에 지시서가 없으면 "지시서 없음" 이라고 말하고 멈춘다.
- 지시서에 적힌 산출 파일 밖을 고치지 않는다. 다른 사람 파일과 공용 파일은 건드리지 않는다.
  (공용: frontend/mockups/mockup.html · docs/01-erd.md · docs/02-api.md · backend/app/models.py)
- 작업 사이마다 git status 를 확인해서 지시서에 없는 파일이 있으면 되돌린다.
```

**주의**: 문구가 마음에 안 들면 Claude에게 어느 템플릿의 어느 문장이 왜 어색한지 짚어서 다시 시킬 것. 뭉뚱그려 "더 좋게"라고 하면 안 좋아진다.

## 검증 방법

4종을 소리 내어 읽어보고, 팀 채널에 올려 팀장 검수를 받는다. 변수 표기가 4종에서 동일한 형식인지 확인.

## PR 체크리스트

- [ ] PR 본문에 4종 요약 한 줄씩
- [ ] 코드·스키마 변경 없음 (문서만)
