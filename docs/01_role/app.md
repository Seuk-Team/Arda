# 앱 로드맵

> **오너**: minahdev · **폴더**: `mobile/` (신설) · **상태**: 확정 v1.1 (2026-08-26, 스택 Flutter 확정 — #16·#18 + ADR-0010)
> 공통 규칙은 [04-team.md](../00_overview/04-team.md). **디자인은 [05-design.md](../00_overview/05-design.md)를 따른다** — 모바일 원칙(§9: 칸반 금지, 터치 44px)과 토큰이 그대로 적용된다.

## 일정 · 운영 (전 도메인 공통)

- **초기 버전: 09/04(금) · 1차 완성: 09/30(수).** 초기 버전의 정의는 [00-overview.md](../00_overview/00-overview.md) 진행 순서 절 — **앱은 초기 버전 범위 밖**이고, 그 시점까지의 기여는 전환기 백엔드 분담(B2·D1)과 스택 ADR-0010이다. 주차 기준: W1 08/24~28 · W2 08/31~09/04 · W3 09/07~11 · W4 09/14~18 · W5 09/21~25 · 09/28~30 통합 버퍼.
- **주 단위로 스스로 계획하고 진행한다.** 아래 주간 계획이 기본값 — 오너가 상황에 맞게 조정하되, 조정하면 이 문서를 갱신하고 다른 도메인에 걸리는 변경만 [06-weekly.md](../00_overview/06-weekly.md)에 적어 팀장과 맞춘다. **주 시작 시 [주간 계획 프롬프트](../00_overview/06-weekly.md#주간-계획-프롬프트--오너용)를 Claude에 붙여넣으면 지난주 점검→이번 주 계획→이 문서 갱신까지 진행된다.**
- **팀장 머지·검수·뒷처리는 매주 금요일 최소 1회** (필요시 주 2~3회 수시). 팀장 승인이 필요한 PR(스키마·02-api·공용 문서·타 도메인 파일)은 금요일 검수에 걸리도록 **목요일까지 올린다.** 자기 도메인 내부 PR은 이 사이클과 무관하게 수시 셀프 머지.

## 1. 미션

담당자·면접관용 **모바일 네이티브 앱**. 이동 중에 지원자를 확인하고 단계를 바꾸고 평가를 남긴다. 웹과 **같은 API**를 쓰는 두 번째 클라이언트 — "API를 제대로 설계하면 클라이언트가 몇 개든 붙는다"를 증명하는 트랙이다.

## 2. 스택 (확정 — [ADR-0010](../03_decision/0010-앱-flutter-확정.md))

**Flutter (Dart).** 실개발·시연은 **Android 전용**, iOS는 코드 호환만 유지한다.

- 자체 렌더링 엔진이라 화면이 OS와 무관하게 동일하고, 위젯 카탈로그가 넓어 서드파티 의존이 적다
- 디자인 토큰·화면 지도(05-design)는 CSS 값 → Dart `ThemeData`로 **수동 이식**한다 (웹의 `frontend/app/src/tokens.css`가 원본)
- **실기기 배포는 Android만** — 팀 Android 4대 + 에뮬레이터가 개발·시연 대상이다
- **iOS는 코드 호환까지만** — iOS 지원 플러그인만 쓰고 플랫폼 분기를 안 남긴다. **Mac이 없어 iOS 빌드 검증은 불가능하며, 이를 "iOS 지원"이라 주장하지 않는다**
- 데모 범위를 Android로 좁힌 전제 위의 확정이다 — 근거와 감수 사항은 [ADR-0010](../03_decision/0010-앱-flutter-확정.md)

## 3. 범위

**포함** — 로그인(JWT) · 공고 리스트 · 지원자 리스트(단계 탭 필터 + 퍼널 바) · 지원자 상세(지원 정보 · **단계 이력**·**평가 목록**은 별도 화면) · **단계 변경**(드래그 대신 확인 시트 — 기획 §2-3) · 평가 작성 · 이력서 열람(presigned URL).

화면 설계 기준: [planning/00_summary_ko.md](../04_planning/00_summary_ko.md) 8장 + `frontend/mockups/mockup-mobile.html` + **모바일 시안(2026-08-28, 프론트 오너 작성)**. 목업에 없던 5건(단계 변경 UI · 단계 이력 · 평가 목록 · 퍼널 바 배치 · 공고 리스트)을 시안이 채웠고, 그 과정에서 기존 화면의 치수도 함께 정해졌다 — **목업과 시안이 어긋나면 시안이 뒤에 온 것이므로 시안을 따른다.**

**제외** — 지원 폼(지원자용 외부 링크는 웹 전용) · 칸반(모바일 금지 원칙) · 앱 내 푸시 알림 G7(△ 여유 — 하게 되면 별도 합의) · 스토어 배포(데모는 Android APK 직접 전달, 여유 시 Play 내부 테스트 트랙까지) · **iOS 실기기 시연**(Mac 부재로 빌드 불가 — ADR-0010).

## 4. 인터페이스 계약

**의존** — 백엔드 API 전부 ([02-api.md](../00_overview/02-api.md)). **앱 때문에 API를 바꾸지 않는 것이 원칙** — 부족한 게 발견되면 백엔드 오너와 인터페이스 PR로 합의(그 자체가 발표 소재다). 인증 A1이 M2의 선행. **제공** — 없음 (순수 소비자. 그래서 격리가 가장 높다 — 앱이 실패해도 다른 파트가 안 죽는다).

## 5. 주간 계획

| 주차 | 단계 | 내용 | 완료 기준 |
|---|---|---|---|
| **W1** (~08/28) | 전환기 착수 + **M1 뼈대 선행 완료** | B2 공고 CRUD ✅ · D1 지원자 API ✅ · D6 직접 등록 ✅ · ADR-0010 스택 확정 ✅ (B1 목업은 cloverky에게 인계됨) · **큐 5번 Flutter 뼈대 ✅** | **B2 #32 · D1 #48 · D6 #72 · ADR-0010 #52·#53 · 스택 전환 #110·#113 · 큐 5번 #114 전부 머지.** 전환기 백엔드 분담 완료. **W2 예정이던 큐 5번을 08/26 에 앞당겨 끝냈다.** 다음은 큐 6번 목데이터 화면 |
| **W2** (~09/04) | **M1 뼈대** | ~~Flutter SDK · 프로젝트 생성 · 토큰 이식 · 내비게이션 뼈대~~ **→ W1 에 완료(#114)**. 남은 것: 팀 Android 기기 전원 APK 설치 확인 · 큐 6번 착수 | Android 실기기 구동 ✅ (SM-S928N / Android 16, `flutter analyze` 무경고 · 테스트 3개 통과). **팀 Android 기기 전원에 APK 설치 확인은 미완** |
| **W3** (~09/11) | **M1 정적 화면** | 전환기 잔여 D6 · 목데이터로 지원자 리스트+상세 · 로그인 화면 | `mockup-mobile.html`·**모바일 시안**과 나란히 놓고 같은 제품으로 보임. 목데이터 필드명 = ERD |
| **W4** (~09/18) | **M2 API 연동** | JWT 로그인(secure storage) · 공고·지원자·상세 실데이터 · **단계 변경** · 평가 작성 · loading/empty/error 3종 | 앱에서 단계 변경 → **웹 칸반에 반영** (같은 API 증명). 면접관 계정은 배정된 지원자만 보임(A3) |
| **W5** (~09/25) | **M3 마감·데모** | 이력서 열람(presigned) · 극단값(긴 이름·태그 다수·자소서 5천 자) · 에러·오프라인 안내 · 데모 시나리오 | **팀 Android 기기 전원에서 APK로 구동.** 데모 시나리오(출근길 서류 검토 → 단계 변경 → 메일 자동 발송 수신)가 끊김 없이 돈다 |
| 09/28~30 | 버퍼 | 데모 동결 · (여유) Play 내부 테스트 트랙 | **09/30 1차 완성** |

## 6. 작업 큐 — 위에서부터 순서대로

| # | 작업 | 선행 |
|---|---|---|
| 1 | ~~(전환기) 백엔드 B2 공고 CRUD~~ — **완료 ✅** ([PR #32](https://github.com/Team-Seuk/Arda/pull/32), 08/25) | J0 · ERD |
| 2 | ~~(전환기) D1 지원자 목록·상세 API~~ — **완료 ✅** ([PR #48](https://github.com/Team-Seuk/Arda/pull/48), 08/25) | 1번 ✅ |
| 3 | ~~(전환기) D6 담당자 직접 등록~~ — **완료 ✅** ([PR #72](https://github.com/Team-Seuk/Arda/pull/72), 08/25) | 2번 ✅ |
| 4 | ~~스택 확정 ADR-0010~~ — **확정 ✅** (#52·#53, 08/25 · 확정자 bestcow) | 없음 |
| 5 | ~~Flutter SDK + 프로젝트 생성 + 토큰 이식(`ThemeData`) + 내비게이션 뼈대~~ — **완료 ✅** ([PR #114](https://github.com/Team-Seuk/Arda/pull/114), 08/26) | 4번 ✅ · ADR-0010 ✅ |
| 6 | 지원자 리스트·상세·로그인·**공고 리스트** (목데이터) + 모바일 시안 반영 | 5번 · 시안 |
| 7 | JWT 로그인 연동 | 백엔드 A1 |
| 8 | 리스트·상세·단계 변경·평가 연동 | 백엔드 M1·M2 |

전환기 1~3번이 앱 자신의 선행조건(D1 지원자 API)을 본인 손으로 푸는 구조였다 — 순서를 지켜 대기가 없었다. **전환기 백엔드 분담은 08/25 로 완료됐다(B2·D1·D6).** 이후 큐는 앱 트랙이다.

> **진행 상태(todo/doing/done)는 [팀 칸반](https://ats.suvisdev.cloud/kanban/)에 적는다** — 이 표는 범위와 순서만 관리한다 ([03-conventions.md](../00_overview/03-conventions.md) 진행 상태 절, 2026-08-28 도입).

**해결된 확인 (2026-08-26)**

- **앱 ID `cloud.seuk.arda` 확정** (팀장 확인). 팀 도메인 `seuk.cloud`를 거꾸로 쓴 값이다. Play 스토어에 올리면 영구 고정되는 값이라 코드가 쌓이기 전에 정했다. GitHub 조직명 기반(`com.teamseuk.arda`)도 후보였으나, **실제 소유한 도메인이 있으면 그것이 규칙상 맞는 근거**라 도메인을 택했다.
- **Flutter SDK가 이미 설치돼 있었다** (`C:\src\flutter`, 3.44.8 · Dart 3.12.2). Android Studio·Android SDK·Flutter/Dart 플러그인도 갖춰져 있어 큐 5번의 "SDK 설치" 항목은 PATH 확인으로 끝났다. `flutter doctor`에서 Visual Studio만 ✗ 인데 **Windows 데스크톱 앱용이라 무시한다**(Android 전용).
- **폰트 조달 방식** — 웹은 Google Fonts CDN을 쓰지만 앱은 `assets/fonts/`에 번들했다. 발표장 네트워크에 의존하지 않기 위해서다(ADR-0010). 굵기는 05-design §2가 실제로 지정한 400·600 2종만(5.4MB) — 웹이 받는 500·700은 문서에 용도가 없다. SIL OFL 1.1이라 번들·배포가 허용되며, 라이선스 사본 배포 요구를 지키려고 `OFL.txt`를 `LicenseRegistry`에 등록했다.

**해결된 확인 (2026-08-25)**

- **D1 relationship** — 지시서 예시가 `selectinload(Application.stage_history)` 를 쓰는데 `models.py` 에 `relationship()` 이 하나도 없었다. 수정 금지 파일이라 팀 채널에 확인 요청했고, 팀장이 자식 4종을 #45 로 추가해 해결. 이후 지시서 예시대로 진행했다.
- **앱 스택 — Flutter 확정** ([ADR-0010](../03_decision/0010-앱-flutter-확정.md), 확정자 bestcow). 08/25에는 같은 판단 기준(학습 비용이 아니라 데모 배포 경로)이 Expo를 가리켰으나, 08/26 팀이 **데모 범위에서 iOS 1대를 제외**하기로 하면서 전제가 바뀌어 Flutter로 확정했다. 기술 비교가 아니라 범위가 바뀐 결정이다.
- **D6 인증 적용 여부** — 지시서는 "인증·권한 코드를 넣지 않는다(아직 없으므로)"고 하나 #61(A3)로 전제가 사라졌다. [02-api.md](../00_overview/02-api.md) 가 이 엔드포인트를 `recruiter+` 로 규정하므로 **API 문서를 기준으로 삼는다**(팀장·백엔드 오너 확인). `require_recruiter` 를 적용했고, `changed_by` 도 `TODO(A1)` 이 아니라 실제 담당자로 채웠다.

## 7. 리스크

- **iOS 빌드 검증 불가** (Mac 없음) → 호환은 코드 규율로만 관리한다: iOS 지원 플러그인만 사용, `Platform.isAndroid` 분기 금지, `flutter analyze` 무경고. **"iOS도 된다"고 말하지 않는다** (ADR-0010).
- **Dart·Flutter 첫 사용 + 팀 내 사용자 0명** → 막히면 전부 혼자 푼다. SDK·`flutter doctor`·실기기 구동은 08/26 에 통과했으므로(#114) 남은 위험은 언어·프레임워크 자체다. 하루 안에 안 풀리면 팀 채널에 올린다.
- **개인 기기·개발 환경 편차** → M1에서 팀 Android 기기 전원에 APK 설치를 먼저 확인하고, 안 되는 기기는 데모 대상에서 뺀다. QR 즉시 반영이 없으므로 **배포 확인을 주 1회로 정기화**한다.
- **웹과 룩이 어긋남** → 토큰을 코드로 이식하고(임의 색 금지), 완료 기준에 목업·시안 대조 포함. **시안에 없는 값이 필요하면 만들지 말고 묻는다**(05-design §12) — 목업에 없던 5건이 그렇게 해결됐다.
- **백엔드 지연** → M1이 전부 API 무관. 전환기 큐로 본인이 선행을 만든다.

## 8. 면접 스토리

- **같은 API로 웹·앱 두 클라이언트** — 클라이언트 추가가 서버 변경 0으로 끝났는가, 아니면 무엇이 부족해 무엇을 바꿨는가
- 모바일에서 칸반을 버리고 리스트+단계 변경 버튼으로 간 **UX 재설계 근거** (기획 §2-3을 본인 언어로)
- **스택을 Flutter로 확정한 근거** ([ADR-0010](../03_decision/0010-앱-flutter-확정.md)) — 데모 범위를 Android로 좁히면서 무엇을 얻고 무엇을 포기했는지(iOS 실기기·웹 코드 재사용·QR 즉시 배포)까지 말할 수 있어야 한다
