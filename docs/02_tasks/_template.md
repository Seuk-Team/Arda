# [기능번호] 제목

> 담당: (이름) · 역할 (A~E) · 브랜치: `feat/<기능번호>-<슬러그>`
> **PR 단위**: (한 사람이 어디까지 끝내고 PR을 올리는지 — 예: "배치 5개 묶음 = PR 1개" / "이 지시서 전체 = PR 1개")

## 배경

이 작업이 왜 필요한지, 전체 그림에서 어디에 끼는지 2~3줄.

## 선행 조건 — 없으면 중단

아래가 하나라도 없으면 **시작하지 말고 팀 채널에 알린다.** 없는 것을 상상해서 만들지 않는다.

- [ ] (예: `backend/app/models/` 에 스키마가 올라와 있을 것)
- [ ] (예: [01-erd.md](../00_overview/01-erd.md)의 해당 테이블이 확정 상태일 것)

선행 조건이 없는 작업이면 "없음"이라고 적는다.

## 완료 조건

- [ ] 측정 가능한 조건 1 (예: "POST /… 호출 시 …가 저장된다")
- [ ] 조건 2
- [ ] 관련 문서 갱신 (해당 시)

## 완성 예시

형식을 글로 설명하지 말고 **완성된 실물 하나**를 여기 붙인다. 나머지는 이것과 똑같이 만든다.

```
(파일 1개 분량의 실제 결과물 — 경로 + 내용)
```

## 진행 원장

여러 명이 배치로 나눠 맡는 작업에만 쓴다. 각자 빈 줄을 골라 담당에 이름을 적고 시작한다. 이어받는 사람은 이 표만 보면 된다. (단일 작업이면 이 섹션째 삭제)

| 배치 | 할 일 | 담당 | 날짜 | 검증 통과 |
|---|---|---|---|---|
| 001 | | | | ☐ |
| 002 | | | | ☐ |

## 참고 문서

- [docs/00_overview/01-erd.md](../00_overview/01-erd.md) 중 관련 테이블: (테이블명)
- [docs/00_overview/02-api.md](../00_overview/02-api.md) 중 관련 엔드포인트: (경로)

## Claude에게 시키기

작업 폴더에서 Claude Code를 열고 아래를 붙여넣는다. (한 번에 안 되면 완료 조건을 하나씩 시킨다)

```
(여기에 팀장이 작성한 프롬프트 — 파일 경로·제약·완료 조건 포함)
```

```
마지막 단계 — 여기서 멈추지 마라.

위 작업을 끝내고 PR 을 올렸으면, 이어서 다음 할 일을 스스로 찾아 그대로 수행해라.

1. docs/00_overview/04-team.md 에서 내 도메인을 확인하고, docs/01_role/<내 도메인>.md 의 작업 큐를 읽어라.
   (작업 큐는 로드맵에만 있다. docs/00_overview/06-weekly.md 는 주 마감 게이트·도메인 간 일정용이다)
2. 큐에서 선행이 풀린 다음 작업을 찾아라. 큐가 비었거나 전부 막혀 있으면
   멈추고 무엇이 막혔는지 팀 채널에 알려라.
3. 찾은 작업의 지시서가 docs/02_tasks/ 에 링크돼 있으면 열고, 선행 조건을 먼저 확인해라.
   선행 조건이 안 채워졌으면 거기서 멈추고 무엇이 막혔는지 알려줘라.
4. 선행 조건이 채워졌으면 새 브랜치를 만들고 그 지시서의 "Claude에게 시키기" 를
   0단계부터 순서대로 수행해라. 지시서 하나 = PR 하나다.
5. 그 작업도 끝나면 1번으로 돌아가 반복해라.

지키는 것:
- 지시서가 있는 작업만 한다. 큐 항목에 지시서가 없으면 "지시서 없음 — 오너가 직접 쪼갤 것"
  이라고 말하고 멈춘다.
- 지시서에 적힌 산출 파일 밖을 고치지 않는다. 다른 도메인 파일과 공용 파일은 건드리지 않는다.
  (공용: frontend/mockups/mockup.html · docs/00_overview/01-erd.md · docs/00_overview/02-api.md · backend/app/models.py)
- 작업 사이마다 git status 를 확인해서 지시서에 없는 파일이 있으면 되돌린다.
```

**주의**: Claude가 스키마 변경이나 담당 폴더 밖 수정을 제안하면 **수락하지 말고** 팀 채널에 물어본다.

## 검증 방법

```bash
(실행·확인 명령)
```

기대 결과: (무엇이 보이면 성공인지)

## 막히면 · 되돌리기

- **30분 넘게 막히면 혼자 붙들지 말고 팀 채널에 묻는다.** 혼자 Claude와 씨름한 2시간이, 팀장이 5분에 답할 것을 이상한 결과물로 바꿔놓는다.
- **결과가 이상해지면 고치려 들지 말고 버린다.** 코드를 읽어 고치는 것보다 되돌리는 편이 빠르고 안전하다.

```bash
git checkout -- .
```

그래도 이상하면 브랜치째 버리고 처음부터 다시 시작한다.

```bash
git checkout main && git branch -D <브랜치명>
```

- **`git status`에 지시서에 없는 파일이 떠 있으면 Claude가 범위를 벗어난 것이다.** 그 변경은 되돌리고 팀 채널에 알린다.

## PR 체크리스트

- [ ] PR 본문에 실행/테스트 결과 붙임
- [ ] 스키마·API 변경 없음 (있다면 머지 전 합의)
