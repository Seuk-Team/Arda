# ADR-0025. 운영 권한을 팀장 개인 계정에서 떼어낸다 — 인수자 woojeongalex

- 상태: **확정** (2026-09-02) — 진행 중인 항목은 아래 체크리스트가 진실
- 작성: 인프라·총괄 (bestcow)
- 선행: [04-team.md](../00_overview/04-team.md) 도메인 오너 표 · [07-deploy.md](../00_overview/07-deploy.md) "주의" 절

## 맥락

프로젝트의 외부 서비스가 전부 **팀장 개인 계정에 묶여 있었다** — AWS(루트·IAM 유저 하나), GitHub org 오너 1명, Vercel 프로젝트, 도메인 `seuk.cloud`, Anthropic·OpenAI 키, Discord 서버. 서버 `.env` 의 AWS 키까지 팀장 개인 IAM 유저 키였다. 팀장이 빠지는 순간 배포·메일·업로드가 멈추고, 아무도 고칠 수 없는 구조다.

## 결정

1. **권한은 사람이 아니라 역할에 붙인다.** 운영 콘솔은 인수자의 IAM 유저, 서버가 쓰는 키는 서버 전용 IAM 유저. 개인 키는 어디에도 남기지 않는다.
2. **AWS 계정은 팀장 명의로 그대로 둔다.** 계정(`cow`, 2350-6367-8285)은 팀장 개인 계정인데, **2026-08-28 학원(ETECH SYSTEM) Organization 에 통합 결제 멤버로 가입**했다 — "$400 지원"의 실체가 이것이다. 멤버로 있는 동안 **모든 요금은 학원 관리 계정에 청구**되고, 그래서 루트로도 결제·예산 화면이 막힌다. 계정을 넘길 수 없는 이유이자 넘길 필요가 없는 이유다. **조직을 먼저 나가면 안 된다** — 나가는 순간 요금이 팀장 카드로 돌아온다. 프로젝트 종료 시 순서: 리소스 삭제 → 학원에 종료 통보(조직에서 제거해 달라고) → 그 뒤 계정 해지. 해지 전 DB 덤프·S3 객체는 팀에 넘긴다.
3. **인수자는 woojeongalex.** 백엔드 오너이자 서버 작업(재배포·DB 이행)을 이미 문서 기준으로 수행할 수 있는 유일한 사람이다. 인프라 도메인 폴더(`infra/` · `.github/` · `docker-compose.yml`)의 오너십도 함께 넘긴다 — [04-team.md](../00_overview/04-team.md) 표 갱신.
4. **시크릿은 인수자가 재발급한다.** 팀장이 만든 값(JWT_SECRET · DB_PASSWORD · Anthropic·OpenAI 키)은 인수자가 자기 계정에서 새로 발급해 서버 `.env` 를 교체한다. 팀장 것은 그 뒤 폐기.

## 체크리스트 (2026-09-02 기준)

| # | 무엇 | 누가 | 상태 |
|---|---|---|---|
| 1 | IAM 유저 `woojeongalex`(콘솔, PowerUserAccess) · `arda-server`(서버 키) | bestcow | ✅ 09/02 |
| 2 | 서버 `.env` AWS 키 → `arda-server`, 팀장 개인 키 비활성화 | bestcow | ✅ 09/02 (`sts` 로 확인) |
| 3 | 서버에만 있던 `docker-compose.prod.yml`·`Caddyfile` → `infra/` | bestcow | ✅ 09/02 |
| 4 | Discord 서버 소유권 | bestcow → woojeongalex | ✅ 09/02 |
| 5 | GitHub org `Team-Seuk` 오너 추가 | bestcow | ✅ 09/02 (`gh api -X PUT orgs/Team-Seuk/memberships/woojeongalex -f role=admin` — `admin:org` scope 필요) |
| 6 | Vercel 프로젝트 `arda-nu` 이전 | bestcow (대시보드 Settings → Transfer) | ⬜ 인수자 Vercel 계정 필요 |
| 7 | 도메인 `seuk.cloud` — 등록기관 계정 이전 또는 인수자 추가 | bestcow | ⬜ 등록기관 확인부터 |
| 8 | ~~AWS Budgets 알림~~ | — | **불필요** — 통합 결제라 결제 화면이 막혀 있고, 비용은 학원 관리 계정이 본다 |
| 9 | Anthropic·OpenAI 키 → 인수자 발급분으로 서버 `.env` 교체 | woojeongalex | ⬜ 서버 세션 1회 |
| 10 | JWT_SECRET · DB_PASSWORD 재발급 | woojeongalex | ⬜ DB 비밀번호는 `ALTER ROLE` + `.env` 두 곳 + 컨테이너 재생성 |
| 11 | 인수자가 재배포 1회를 혼자 성공 | woojeongalex | ⬜ 이게 끝나야 이관 완료 |
| 12 | 프로젝트 종료 시 AWS 리소스 삭제 → 학원에 종료 통보(조직 제거 요청) → 계정 해지. **순서 바꾸면 요금이 개인으로 온다** | bestcow | ⬜ 발표 뒤 |

## 대안 — AWS 계정을 통째로 넘기지 않는 이유

- **루트 이메일만 교체**: 가능은 하지만 실익이 없다 — 통합 결제 동안 요금이 개인에 안 걸려 있고, 학원과의 지원 관계가 팀장 이름으로 맺어져 있어 바꾸면 그쪽과 다시 합의해야 한다.
- **인수자 계정에 재구축**: DB 덤프 + S3 sync + EIP 이전으로 가능하고 스택이 작아 반나절이면 된다. 그러나 **SES 가 다시 샌드박스**라 도메인 재검증·해제 신청을 처음부터 해야 하고(08/27 한 번 거절된 전력), 크레딧을 버린다.
- 그래서 **계정은 두고 권한만 넘긴다.** 데모 기간이 짧고 크레딧이 비용을 덮는 동안은 이 편이 싸다. 종료 시점에 재구축이 필요해지면 위 두 번째 안의 절차를 그대로 쓴다.

## 결과

- 팀장 개인 키가 서버에서 사라졌다. 키 유출 시 재발급 범위가 `arda-server` 하나로 좁다.
- 남은 항목 5~11 은 대부분 콘솔·대시보드 조작이라 Claude 가 대신 못 한다(보안 설정 변경은 자동 모드에서 차단). 명령·경로는 위 표에 적어 두었다.
