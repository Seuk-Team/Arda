# 배포 빌드에서 브라우저가 API 를 직접 부른다 (Vercel rewrite 경유 제거).
# 백엔드 CORS(29da655)가 배포된 뒤에만 동작한다 — 그 전에 이 값을 넣으면
# preflight 에서 막힌다.
# 2026-09-07: 인프라 이전(09-04) 뒤 정본은 api.seuk.suvisdev.cloud 다. 옛 api.arda.seuk.cloud 는
# 이탈한 전 팀장 명의 AWS·도메인에 남아 있는 서버라 언제 꺼질지 모른다. Vercel 배포본은
# 이미 새 주소를 부르고 있었고(번들 실측), 이 파일이 뒤처져 있던 것을 맞춘다.
VITE_API_BASE=https://api.seuk.suvisdev.cloud
