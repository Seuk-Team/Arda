export default function Settings() {
  return (
    <div>
      <h1 style={{ fontSize: 'var(--font-h1)', fontWeight: 700 }}>설정</h1>
      <p style={{ color: 'var(--text-sub)', marginTop: 'var(--sp-3)' }}>
        시스템 설정이 여기에 표시됩니다.
      </p>
    </div>
  )
}
