[project]
name = "arda-backend"
version = "0.1.0"
description = "Arda ATS 백엔드 (FastAPI)"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
