"""도메인 밖 계약 (Ports).

- `output/` : 도메인이 밖에 요구하는 것 (Repository·LLM·Mail·S3).
- `input/`  : 밖이 도메인을 부르는 것 (UseCase — 필요할 때만 만든다).

ADR-0035 참조. 지금은 **선언용 이름 공간**이고, 실제 정의는 각 모듈에서 그 자리
코드(예: `agent/backends/base.py`) 를 re-export 하는 얇은 껍데기다. 새 코드는 여기
정식 이름을 쓰고, 옛 코드는 옛 자리에서 그대로 동작한다 — 파일 삭제 없이 이관.
"""
