# Arda 작업 규칙

> 2026-08-28 개정: 리뷰·사전 확인 게이트 폐지 — 전원 main 직접 push, 사후 공지 체계. 상세는 [docs/00_overview/03-conventions.md](docs/00_overview/03-conventions.md).

## 시작 전

- **자기 도메인의 로드맵**(`docs/01_role/<도메인>.md`)을 먼저 읽는다. 도메인·오너 표는 [docs/00_overview/04-team.md](docs/00_overview/04-team.md). 작업에 **지시서**(`docs/02_tasks/`)가 링크돼 있으면 그것도 읽고 그대로 따른다 — 자기 도메인 지시서는 오너가 직접 쓴다.
- [docs/00_overview/01-erd.md](docs/00_overview/01-erd.md)(테이블 정의서)와 [docs/00_overview/02-api.md](docs/00_overview/02-api.md)를 읽고 시작한다.
- **프론트/UI 작업은 [docs/00_overview/05-design.md](docs/00_overview/05-design.md)를 먼저 읽는다.** 값 미확정("(시안에서 확정)") 토큰은 임의로 채우지 않는다.

## 범위

- **자기 도메인 안이면 묻지 않고 진행한다.** 로드맵·지시서 범위 안의 판단은 오너가 스스로 내린다 — 팀장 확인 절차 없음. 팀 채널에 올릴 것은 도메인 밖뿐: 마일스톤·범위 변경, 다른 도메인이 대기 중인 의존, 30분 넘게 막힌 문제.
- **기본은 자기 도메인 폴더** ([04-team.md](docs/00_overview/04-team.md) 표). 남의 폴더를 고쳤으면 커밋 메시지에 명시하고 팀 채널에 한 줄 공지한다(사후). 큰 변경이면 직접 고치지 말고 이슈로 오너에게 넘긴다.
- **스키마 변경은 자유 — 단 코드와 같은 커밋에서 [01-erd.md](docs/00_overview/01-erd.md)를 갱신하고 팀 채널에 공지한다.** 문서 갱신 없는 스키마 변경 금지: ERD가 전원의 계약이다. API 변경도 동일하게 [02-api.md](docs/00_overview/02-api.md)를 같은 커밋에서 갱신.
- 로드맵·지시서에 없는 기능 추가·리팩터링 금지. 시킨 것만 한다.
- **문서(`*.md`)는 바로 고친다 — 사전 확인 절차 없음.** 공용 문서(`docs/00_overview/`)를 고치면 팀 채널에 사후 한 줄 공지.

## 반영 방식

- **main에 바로 push한다 (전원). 리뷰·승인 없음.** push 전 `git pull --rebase` + 아래 검증. PR은 피드백을 받고 싶을 때만(선택) — 승인 없이 셀프 머지.
- 커밋 제목에 기능 번호(D3, G1, J7…)를 붙인다. 번호 없는 작업은 도메인명 접두어 — 예: `feat(front): React 라우팅 뼈대`. **리뷰가 없으므로 커밋 이력이 곧 기록이다.**

## 금지

- 시크릿(AWS 키, DB 비밀번호, 토큰)을 코드·커밋·로그에 남기지 않는다. 환경변수는 `.env`(git 제외) + `.env.example`(키 이름만).
- 테스트를 통과시키려고 주석 처리하거나 우회하지 않는다. 실패하면 실패한 대로 커밋 메시지·팀 채널에 적는다.

## 검증

- 코드 변경 후 해당 파트의 실행/테스트를 돌려 깨지지 않았는지 확인하고, 결과를 커밋 메시지에 한 줄 남긴다.
- **깨진 main을 발견하면 묻지 말고 고치거나(fix-forward) revert한다.**
