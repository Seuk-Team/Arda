# Arda 작업 규칙

## 시작 전

- 자기 작업의 **지시서**(`docs/tasks/<기능번호>-*.md`)를 먼저 읽는다. 지시서 없는 작업은 팀장에게 요청해 받는다.
- [docs/01-erd.md](docs/01-erd.md)(테이블 정의서)와 [docs/02-api.md](docs/02-api.md)를 읽고 시작한다.
- **프론트/UI 작업은 [docs/05-design.md](docs/05-design.md)를 먼저 읽는다.** 값 미확정("(시안에서 확정)") 토큰은 임의로 채우지 않는다.

## 범위 (가장 중요)

- **자기 담당 모듈/폴더 밖의 파일을 수정하지 않는다.** 다른 모듈에 문제를 발견하면 고치지 말고 이슈로 남긴다.
- **스키마(`docs/01-erd.md`)는 팀장 외 변경 금지.** 테이블·컬럼 추가/변경이 필요하면 기록만 한다.
- 지시서에 없는 기능 추가·리팩터링 금지. 시킨 것만 한다.
- **문서(`*.md`)는 누구나 수정할 수 있지만, 고치기 전에 확인 절차를 거친다.** 어디를 어떻게 고칠지와 그 이유를 먼저 설명하고(팀 채널 또는 PR 본문), 확인받은 뒤 반영한다. Claude에게 시킬 때도 동일 — 바로 고치게 하지 말고 수정안부터 받아서 본다.

## 반영 방식

- `main` 직접 커밋 금지. 브랜치 → PR → 팀장 리뷰 승인 → 머지. 규칙은 [docs/03-conventions.md](docs/03-conventions.md).
- 커밋·이슈·PR 제목에 기능 번호(D3, G1, J7…)를 붙인다.

## 금지

- 시크릿(AWS 키, DB 비밀번호, 토큰)을 코드·커밋·로그에 남기지 않는다. 환경변수는 `.env`(git 제외) + `.env.example`(키 이름만).
- 테스트를 통과시키려고 주석 처리하거나 우회하지 않는다. 실패하면 실패한 대로 PR에 적는다.

## 검증

- 코드 변경 후 해당 파트의 실행/테스트를 돌려 깨지지 않았는지 확인하고, 결과를 PR 본문에 적는다.